let p = document.getElementsByTagName("p")[0];
p.textContent;

let ul = document.querySelector("ul");
ul.textContent; //doesn't maintain HTML structure

p.innerHTML; //preserves HTML structure like strong tag
ul.innerHTML;

document.querySelector("h1").textContent = "NICE WE FINISHED LMAO"

//Manipulating Attributes ******
